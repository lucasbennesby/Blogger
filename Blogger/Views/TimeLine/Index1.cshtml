@using Blogger.Models
@using System.Security.Claims
@model IEnumerable<Publicacao>

@{
    var usuarioAutenticado = ViewBag.UsuarioLogado;
    var userId = ViewBag.UsuarioId;
}


<style>
    :root {
        --primary-color: #1DA1F2;
        --background-color: #FFFFFF;
        --text-color: #14171A;
        --secondary-text-color: #657786;
        --border-color: #E1E8ED;
    }

    body {
        background-color: var(--background-color);
        color: var(--text-color);
    }

    .text-primary {
        color: var(--primary-color) !important;
    }

    .btn-primary {
        background-color: var(--primary-color) !important;
        border-color: var(--primary-color) !important;
    }

    .like-star {
        color: var(--primary-color);
    }

    .bi-star-fill {
        color: var(--primary-color);
    }

    .nav-link {
        padding: 10px 15px;
        border-radius: 30px;
        transition: background-color 0.2s;
        margin-bottom: 8px;
        font-size: 1.1rem;
    }

        .nav-link:hover {
            background-color: rgba(29, 161, 242, 0.1);
            border-radius: 30px;
        }

        .nav-link i {
            font-size: 1.3rem;
            margin-right: 12px;
        }

    .sticky-top {
        top: 0;
        z-index: 1020;
    }
</style>

<div class="container-fluid">
    <div class="row">
        <!-- Left Sidebar -->
        <div class="col-md-3 d-none d-md-block border-end vh-100 position-fixed start-0" style="width: 280px;">
            <div class="d-flex flex-column h-100 p-3">
                <h1 class="h4 mb-4 fw-bold text-primary ps-2">Blogger</h1>
                <div class="nav flex-column">
                    <a href="#" class="nav-link mb-3 fw-bold d-flex align-items-center ps-2">
                        <i class="bi bi-house-door me-3"></i>
                        <span>Home</span>
                    </a>
                    <a href="#" class="nav-link mb-3 d-flex align-items-center ps-2">
                        <i class="bi bi-hash me-3"></i>
                        <span>Explore</span>
                    </a>
                    <a href="#" class="nav-link mb-3 d-flex align-items-center ps-2">
                        <i class="bi bi-bell me-3"></i>
                        <span>Notifications</span>
                    </a>
                    <a href="#" class="nav-link mb-3 d-flex align-items-center ps-2">
                        <i class="bi bi-envelope me-3"></i>
                        <span>Messages</span>
                    </a>
                    <a href="#" class="nav-link mb-3 d-flex align-items-center ps-2">
                        <i class="bi bi-bookmark me-3"></i>
                        <span>Bookmarks</span>
                    </a>
                    <a href="#" class="nav-link mb-3 d-flex align-items-center ps-2">
                        <i class="bi bi-person me-3"></i>
                        <span>Profile</span>
                    </a>
                </div>
                <button class="btn btn-primary rounded-pill w-100 mt-3">Tweet</button>
            </div>
        </div>

        <!-- Main Content -->
        <div class="col-md-6 mx-auto" style="margin-left: 280px;">
            <div class="border-bottom p-3">
                <h2 class="h5 mb-0 fw-bold">Home</h2>
                <div id="app">
                    @if (Model.Count() > 0)
                    {
                        @foreach (var p in Model)
                        {
                            <a asp-action="Detalhes" asp-controller="Publicacao" asp-route-id="@p.Id">
                                <div class="container my-4">
                                    <div class="">
                                        <!-- Linha principal: Foto de perfil ao lado do conteúdo -->
                                        <div class="d-flex">
                                            <!-- Foto de perfil -->
                                            <div class="me-3">
                                                <img src="/imagens/@p.Imagem" width="40" height="40" alt="Foto de Perfil" class="rounded-circle position-absolute">
                                            </div>
                                            <!-- Conteúdo do post -->
                                            <div>
                                                <div class="ms-5">
                                                    <strong>@p.NomeAutor</strong> <span class="text-muted">  @@@p.NomeAutor · @p.HoraAteAgora(@p.Data)`</span>
                                                </div>
                                                <div class="mb-1 ms-5">
                                                    @Html.Raw(@p.Conteudo)
                                                </div>
                                                <!-- Botões de ações -->
                                                <div class="d-flex  mt-3">
                                                    <a href="#" class="ms-5 ">
                                                        <i class="bi bi-chat"></i>
                                                    </a>

                                                    <input hidden value="@p.Id" name="idUsuario" />
                                                    <input hidden value="@userId" name="idPublicacao" />
                                                    <button v-on:click="darLike(@p.Id,$event)" class="ms-5 like-star" style="border:none; background-color:transparent;color:#2683B9;">
                                                        <i id="star-@p.Id" :class="isLiked(@p.Id) ? 'bi bi-star-fill' : 'bi bi-star'"></i>
                                                        <span class="like-number" id="likeCounter-@p.Id">@p.ContadorDeLikes()</span>
                                                    </button>
                                                    <div href="#" class="ms-5">
                                                        <i class="bi bi-share"></i>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <hr />
                            </a>
                        }
                    }
                    else
                    {
                        <h1>Não existem publicações!</h1>
                    }
                </div>
            </div>

            <!-- Tweet Composer -->
            @* <div class="p-3 border-bottom">
                    <div class="d-flex">
                        <img src="https://via.placeholder.com/48" class="rounded-circle me-2" alt="Profile">
                        <div class="flex-grow-1">
                            <textarea class="form-control border-0 mb-2" placeholder="What's happening?" rows="3"></textarea>
                            <div class="d-flex justify-content-between align-items-center">
                                <div>
                                    <!-- Tweet attachment icons would go here -->
                                </div>
                                <button class="btn btn-primary rounded-pill px-3">Tweet</button>
                            </div>
                        </div>
                    </div>
                </div> *@

            <!-- Timeline -->
            <div id="timeline">
                <!-- Tweet 1 -->

            </div>

            <!-- Right Sidebar - Adjusted -->
            @*  <div class="col-md-3 d-none d-lg-block position-fixed end-0 vh-100 overflow-auto" style="width: 290px; top: 0;">
                <div class="p-3 h-100">
                    <div class="sticky-top pt-3 pb-2 bg-white">
                        <div class="input-group mb-3">
                            <span class="input-group-text bg-light border-end-0">
                                <i class="bi bi-search"></i>
                            </span>
                            <input type="text" class="form-control bg-light border-start-0" placeholder="Search">
                        </div>
                    </div>

                    <!-- Trending section -->
                    <div class="bg-light rounded p-3 mb-3">
                        <h3 class="h5 mb-3 fw-bold">Trends for you</h3>

                        <div class="mb-3">
                            <div class="d-flex justify-content-between">
                                <small class="text-muted">Technology · Trending</small>
                                <i class="bi bi-three-dots text-muted"></i>
                            </div>
                            <p class="mb-0 fw-bold">#JavaScript</p>
                            <small class="text-muted">125K tweets</small>
                        </div>

                        <div class="mb-3">
                            <div class="d-flex justify-content-between">
                                <small class="text-muted">Business · Trending</small>
                                <i class="bi bi-three-dots text-muted"></i>
                            </div>
                            <p class="mb-0 fw-bold">#Startup</p>
                            <small class="text-muted">98K tweets</small>
                        </div>

                        <div class="mb-3">
                            <div class="d-flex justify-content-between">
                                <small class="text-muted">Sports · Trending</small>
                                <i class="bi bi-three-dots text-muted"></i>
                            </div>
                            <p class="mb-0 fw-bold">World Cup</p>
                            <small class="text-muted">255K tweets</small>
                        </div>

                        <div class="mb-3">
                            <div class="d-flex justify-content-between">
                                <small class="text-muted">Entertainment · Trending</small>
                                <i class="bi bi-three-dots text-muted"></i>
                            </div>
                            <p class="mb-0 fw-bold">#MovieNight</p>
                            <small class="text-muted">45K tweets</small>
                        </div>

                        <a href="#" class="text-decoration-none">Show more</a>
                    </div>

                    <!-- Who to follow -->
                    <div class="bg-light rounded p-3">
                        <h3 class="h5 mb-3 fw-bold">Who to follow</h3>

                        <div class="d-flex justify-content-between mb-3 align-items-center">
                            <div class="d-flex align-items-center">
                                <img src="https://via.placeholder.com/40" class="rounded-circle me-2" alt="Tech Insider">
                                <div>
                                    <div class="fw-bold">Tech Insider</div>
                                    <div class="text-muted">techinsider</div>
                                </div>
                            </div>
                            <button class="btn btn-dark btn-sm rounded-pill">Follow</button>
                        </div>

                        <div class="d-flex justify-content-between mb-3 align-items-center">
                            <div class="d-flex align-items-center">
                                <img src="https://via.placeholder.com/40" class="rounded-circle me-2" alt="Web Dev Tips">
                                <div>
                                    <div class="fw-bold">Web Dev Tips</div>
                                    <div class="text-muted">webdevtips</div>
                                </div>
                            </div>
                            <button class="btn btn-dark btn-sm rounded-pill">Follow</button>
                        </div>

                        <div class="d-flex justify-content-between mb-3 align-items-center">
                            <div class="d-flex align-items-center">
                                <img src="https://via.placeholder.com/40" class="rounded-circle me-2" alt="Coding News">
                                <div>
                                    <div class="fw-bold">Coding News</div>
                                    <div class="text-muted">codingnews</div>
                                </div>
                            </div>
                            <button class="btn btn-dark btn-sm rounded-pill">Follow</button>
                        </div>

                        <a href="#" class="text-decoration-none">Show more</a>
                    </div>
                </div>
            </div> *@
        </div>
    </div>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <script>
            const app = Vue.createApp({
            data() {
                return {
                    publicacoes: @Html.Raw(Json.Serialize(Model)),
                    usuarioId: '@userId'
                }
            },
            mounted() {

            },
            methods: {
                async darLike(publicacaoId, event) {

                    const response = await fetch(`/TimeLine/LikeTimeLine`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                                        body: JSON.stringify({ PublicacaoIdVM : publicacaoId, UsuarioIdVM: this.usuarioId })
                    })

                        this.toggleStar(publicacaoId,event)

                },
                isLiked(publicacaoId, event) {
                    let post = this.publicacoes.find(p => p.id == publicacaoId);
                    return post.likes.some(like => like.usuarioId == this.usuarioId)
                },
                   toggleStar(id, event) {

                let star = document.getElementById('star-'+id);

                let counter = document.getElementById('likeCounter-'+id); // ID do contador


                     if (star.classList.contains('bi-star-fill')) {
                           pop(event);
                           star.classList.replace('bi-star-fill', 'bi-star');
                           counter.textContent = parseInt(counter.textContent, 10) - 1;
                     } else {
                            pop(event);
                           star.classList.replace('bi-star', 'bi-star-fill');
                           counter.textContent = parseInt(counter.textContent, 10) + 1;
                     }
            }

            }
        })

        app.mount('#app');
    </script>


    <script>
            document.addEventListener('DOMContentLoaded', function () {
            var type = '@TempData["NotificationType"]';
            var message = '@TempData["NotificationMessage"]';

            if (type && message) {
                new Noty({
                    type: type.trim(),
                    text: message.trim(),
                    timeout: 3000,
                    progressBar: true,
                    layout: 'topLeft'
                }).show();
            }
        });
    </script>

    <script>
        let publicacaoId = null;

            function GuardarIdPublicacao(id) {
                publicacaoId = id;
            }

            function RecarregarPagina() {
                window.location.href = '/Publicacao/Index';
            }

            function abrirModal() {
                var modalElement = document.getElementById('successModal');
                var modal = new bootstrap.Modal(modalElement);
                modal.show(); // Chama a modal
            }

            async function confirmarExclusao() {
                let id = publicacaoId;
                let response = await fetch(`/Publicacao/Deletar?id=${id}`);

                if (response.ok) {
                    abrirModal();

                } else {
                    alert("ocorreu um erro")
                }
            }
    </script>


    <script src="~/js/TimeLine.js" asp-append-version="true"></script>
